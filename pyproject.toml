[project]
name = "lte-analyzer"
version = "0.1.0"
description = "A tool for analyzing LTE Signal Scans"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=2.3.2",
]
